import React from 'react';
import {Suspense, memo} from 'react';

const Home = memo(() => {
    return (
        <div className={''}>
            <div>

            </div>
        </div>
    )
})

export default Home;